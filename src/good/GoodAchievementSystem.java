package good;

import shared.AchievementSystem;

public class GoodAchievementSystem extends Colleague implements AchievementSystem {
    public GoodAchievementSystem(Mediator mediator) {
        super(mediator);
    }
}